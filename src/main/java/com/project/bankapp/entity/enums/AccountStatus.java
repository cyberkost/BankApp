package com.project.bankapp.entity.enums;

/**
 * Enumeration representing the possible status values for an account.
 * This enum defines various states that an account can be in.
 */
public enum AccountStatus {
    ACTIVE,
    BLOCKED,
    CLOSED,
    OVERDUE,
    PENDING
}
