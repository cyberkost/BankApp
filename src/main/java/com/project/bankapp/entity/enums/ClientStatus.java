package com.project.bankapp.entity.enums;

/**
 * Enumeration representing the possible statuses of a client in the system.
 * The ClientStatus enum defines different states that a client account can be in.
 */
public enum ClientStatus {
    ACTIVE,
    BLOCKED,
    VIP
}
