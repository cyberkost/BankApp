package com.project.bankapp.entity.enums;

/**
 * Enum representing different roles or permissions that users can have in the system.
 * Each role defines a specific level of access and functionality.
 */
public enum Roles {
    USER,
    MANAGER,
    ADMIN
}
