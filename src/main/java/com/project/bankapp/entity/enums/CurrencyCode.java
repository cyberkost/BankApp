package com.project.bankapp.entity.enums;

/**
 * Enum representing currency codes.
 * Each currency code corresponds to a specific currency used in financial transactions.
 */
public enum CurrencyCode {
    EUR,
    USD,
    JPY,
    GBP,
    AUD
}
